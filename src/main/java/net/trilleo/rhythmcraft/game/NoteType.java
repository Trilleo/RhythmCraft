package net.trilleo.rhythmcraft.game;

public enum NoteType {
    /** A short tap — press the key once when the note reaches the judgment line. */
    TAP,
    /** A sustained hold — press and hold the key from the head until the tail passes. */
    HOLD
}
